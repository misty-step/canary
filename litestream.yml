dbs:
  - path: /data/canary.db
    replicas:
      - type: s3
        bucket: ${BUCKET_NAME}
        path: canary.db
        endpoint: https://fly.storage.tigris.dev
        region: auto
        force-path-style: true
        snapshot-interval: 1h
