dbs:
  - path: /data/canary.db
    replicas:
      - type: s3
        bucket: ${BUCKET_NAME}
        path: canary.db
        endpoint: ${LITESTREAM_ENDPOINT}
        region: ${LITESTREAM_REGION}
        force-path-style: true
        snapshot-interval: 1h
