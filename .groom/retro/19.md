## Entry: #19 -- [P0] Add external dead-man's-switch for Canary itself (2026-03-15)

**Effort:** predicted s -> actual s
**Scope changes:** none — issue said "no code changes" and that held true
**Blockers:** none
**Pattern:** GHA cron is a zero-cost external watchdog for single-service monitoring. Concurrency control (`cancel-in-progress: false`) eliminates TOCTOU races in check-then-act workflows. Label creation should always be idempotent (`gh label create ... || true`) since labels can be deleted between runs.

---
