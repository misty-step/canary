*.db
*.db-shm
*.db-wal
!crates/canary-store/tests/fixtures/*.db
/tmp/
.env
.env.*
!.env.example
*.secret

# Secret material — keys, certificates, credentials. Never commit.
*.pem
*.key
*.crt
*.cer
*.der
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
id_ecdsa
id_ecdsa.*
*_rsa
*_ed25519
*_ecdsa
credentials.json
credentials*.json
service-account*.json
*.tfvars
!*.tfvars.example

# Thinktank research outputs (not part of the project)
/gentle-working-tundra/
/polished-marching-river/
/sunlit-moving-walnut/
thinktank.log

# Dependencies
/node_modules/

# Rust build output
/target/

# Legacy Elixir build output from pre-Rust branches
/_build/
/deps/
/canary_sdk/_build/
/canary_sdk/deps/
/canary_sdk/cover/
/cover/
erl_crash.dump

# OS
.DS_Store

# Local Spellbook traces are transient. Harness source and provider rosters live
# in the globally configured Spellbook install, not as repo-local vendored code.
/.spellbook/*/

# Local provider dispatch transcripts are receipt evidence, not project source.
/.harness-kit/traces/provider-lanes/

# Generated artifacts
design-catalog.html
.claude/scheduled_tasks.lock
.antigravitycli/
