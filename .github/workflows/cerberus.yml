# Cerberus AI Code Review
# https://github.com/misty-step/cerberus
name: Cerberus

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, converted_to_draft]

concurrency:
  group: cerberus-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  review:
    uses: misty-step/cerberus/.github/workflows/cerberus.yml@master
    permissions:
      contents: read
      issues: write
      pull-requests: write
    with:
      context: "Self-hosted observability service (Elixir/Phoenix + SQLite). Monorepo: core service at root with Elixir and TypeScript SDK packages. Thin LiveView operator console, signed webhooks, and no embedded triage service."
    secrets:
      api-key: ${{ secrets.CERBERUS_OPENROUTER_API_KEY }}
